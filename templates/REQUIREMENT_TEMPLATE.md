```yaml
requirement:
  id: REQ-<DOM>-<NNN>
  level: <MUST|MUST_NOT|SHOULD|SHOULD_NOT|MAY>
  classification: <mandatory|recommended|optional|conditional>
  condition: "<falls conditional, z.B. criticality >= S3>"
  description: "<praegnante, pruefbare Anforderung>"
```

Compliance-Ergebnis je Requirement: PASS | FAIL | PARTIAL | N/A
